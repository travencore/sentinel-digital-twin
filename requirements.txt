# Supply Chain Digital Twin — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Core SDV stack ──────────────────────────────────────────────────────────
sdv>=1.12.0,<2.0.0          # Synthetic Data Vault (HMASynthesizer, GaussianCopula)
sdmetrics>=0.14.0            # Quality / diagnostic reports shipped with SDV

# ── Numerical / scientific ───────────────────────────────────────────────────
numpy>=1.26.0,<3.0.0
pandas>=2.1.0,<3.0.0
scipy>=1.12.0

# ── Memgraph / graph drivers (pick one) ─────────────────────────────────────
# mgclient is the native Memgraph Python client (Bolt protocol)
mgclient>=1.3.0              # pip install mgclient
# neo4j>=5.0.0               # alternative: neo4j official driver also works

# ── Sentinel Agent (LangGraph) ───────────────────────────────────────────────
langgraph>=0.2.0             # LangGraph state-machine framework
langchain-core>=0.2.0        # BaseMessage, tool primitives used by LangGraph
neo4j>=5.18.0                # Bolt driver for Memgraph (same protocol as Neo4j)

# ── Dashboard ────────────────────────────────────────────────────────────────
streamlit>=1.35.0            # dashboard framework
plotly>=5.22.0               # interactive charts

# ── Utilities ────────────────────────────────────────────────────────────────
python-dateutil>=2.8.2
tqdm>=4.66.0                 # progress bars used by SDV internally
